dependencies:
  # The project targets ESP-IDF v5.1+; avoid requiring a newer toolkit than the
  # installed one so developers can build with the standard release toolchains.
  idf: ">=5.1"
  lvgl/lvgl: "^8.3"
